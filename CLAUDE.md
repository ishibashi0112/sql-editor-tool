# CLAUDE.md

社内 DB（SQL Server / Oracle）向けの、A5:SQL Mk-2 代替 GUI 検索ツール（VS Code 拡張、仮称未定）のリポジトリです。

## 最初に読むもの

- `docs/handover.md`：経緯、決定事項（D-xx）、未確定事項（O-xx）、段階計画、WHERE 生成の仕様。作業を始める前に必ず読むこと。
- 決定や確認結果が出たら `docs/handover.md` を更新し、決定事項は D 番号、未確定事項は O 番号で管理する。

## 前提

- 開発は Mac で行う。社内 DB には Mac から接続できない。DB に繋ぐ検証は会社 PC で行い、結果はユーザーがマスクして持ち帰る。
- DB に依存しないロジック（方言、WHERE 生成）は core パッケージに置き、単体テストで検証する。

## 守ること

- SQL は必ずパラメータ化する。値を文字列連結しない。
- 実行するのは SELECT / WITH のみ（読み取り専用）。
- パスワードは表示・ログ出力しない。
- 社内のホスト名・IP・DB 名・スキーマ名・業務 SQL・業務データを、コード、テスト、ドキュメント、コミットに含めない。例やテストでは架空の名前を使う。
- `.env` と `verify/out/` はコミットしない。
